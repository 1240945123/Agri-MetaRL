\documentclass[authoryear,preprint,review,12pt]{elsarticle}

%% 基础宏包
\usepackage[UTF8]{ctex}
\usepackage{amssymb}
\usepackage{amsmath}
\usepackage{graphicx}
\usepackage{grffile}
\usepackage{booktabs}
\usepackage{lineno}
\usepackage{float}
\usepackage{hyperref}
\usepackage{doi}

%% 设定期刊名称
\journal{Computers and Electronics in Agriculture}

\begin{document}

\begin{frontmatter}

%% 论文题目
\title{Agri-MetaRL：面向温室气候控制的农业元强化学习算法}

%% 作者信息
\author[inst1]{First Author\corref{cor1}}
\ead{author1@email.com}
\cortext[cor1]{通讯作者}

\author[inst1,inst2]{Second Author}
\author[inst2]{Third Author}

%% 作者单位
\affiliation[inst1]{organization={Department of Agricultural Engineering, University Name},
            addressline={Street Address},
            city={City Name},
            postcode={000000},
            state={State},
            country={Country}}

\affiliation[inst2]{organization={Research Institute of Smart Agriculture},
            addressline={Street Address},
            city={City Name},
            postcode={000000},
            state={State},
            country={Country}}

%% 摘要
\begin{abstract}
温室气候控制是智慧农业中的重要问题。该问题的目标有两个：一是保证作物生长环境合适，二是减少能源消耗并提高经济收益。温室系统有三个特点：强非线性、外部扰动随时间变化、作物生长阶段会变化。这些特点使传统方法容易失效。传统方法包括 PID、规则控制和 MPC。MPC 高度依赖精确模型。在跨季节、跨天气条件下，这些方法的控制效果容易变差。强化学习为温室控制提供了一条数据驱动的路径。但现有研究大多在单一环境下训练单一策略。这些研究对跨任务迁移和快速适应关注不足。

针对上述问题，本文将温室控制表述为一组相关任务。这些任务由天气年份、起始日期和场景条件共同定义。本文提出新的元强化学习算法 Agri-MetaRL。该算法不是简单应用现成 RL 做温室控制，而是在算法层面改进了优势估计环节。本文以 Recurrent PPO 为基础，在策略更新前引入支持集-查询集式的优势修正机制。具体而言，本文设计并实现了自研模块 MetaAdvantageHead。该模块通过支持轨迹编码任务上下文，并在查询轨迹上对广义优势估计进行任务自适应修正。本文不引入额外策略分支，也不引入重型双层优化器。在此前提下，本文提高了策略面对任务分布变化时的适应能力。

实验在 GreenLight-Gym 温室基准环境上开展。在 5 次随机种子统计下，Agri-MetaRL 的最终平均训练回报达到 3955。该值高于 Recurrent PPO 的 3886 和 PPO 的 3811。在 held-out 天气分布上，以 Agri-MetaRL 为例，训练任务与测试任务的每种子平均回报分别为 $3955 \pm 16$ 和 $4022 \pm 83$。测试分布上表现更优。在固定协议下，Agri-MetaRL 在温度、湿度和 CO$_2$ 违规步数上均最低。Agri-MetaRL 获得 3.46 €/m$^2$ 的最高 EPI。该值高于 Recurrent PPO 的 3.34 €/m$^2$ 和 PPO 的 3.29 €/m$^2$。
\end{abstract}

%% 图形摘要
\begin{graphicalabstract}
%\includegraphics{figures/graphical_abstract.png}
\end{graphicalabstract}

%% 研究亮点（每条不超过 85 字符）
\begin{highlights}
\item 将温室气候控制建模为天气条件驱动的任务分布，并给出农业元强化学习问题定义。
\item 提出新的元强化学习算法：在 Recurrent PPO 上引入自研 MetaAdvantageHead，实现优势层面的任务自适应修正。
\item 构建 train-task 与 held-out task、zero-shot 一体化的评估协议。
\item Agri-MetaRL 获得更高训练回报（3955）和最高 EPI（3.46 €/m$^2$），在 held-out 天气下保持稳定泛化。
\item 方法在温度控制和跨任务适应方面具有优势。
\end{highlights}

%% 关键词
\begin{keyword}
温室气候控制 \sep 强化学习 \sep 元强化学习 \sep Recurrent PPO \sep 智慧农业
\end{keyword}

\end{frontmatter}

\linenumbers

\section{引言}
设施农业通过调控温度、湿度、CO$_2$ 浓度与辐射条件，为作物提供稳定的生长环境。该方式可提升单位面积产量、延长供应周期并抵御极端天气（Van Henten 2006; Li et al. 2023）。现代高技术温室中，环境变量、作物状态和执行器之间强耦合。外部天气与作物生长阶段持续变化。因此，温室控制具有强时变、部分可观测和多目标优化等特征。如何在保证作物安全边界的前提下实现高收益、低能耗和强泛化控制，是农业工程与智能控制交叉领域的核心问题之一。

传统温室控制常采用 PID、阈值规则和人工经验策略（Berenguel et al. 1997; Van Straten et al. 2010）。这类方法实现简单、解释性强。但参数通常针对特定季节、特定温室和特定作物阶段调优。当天气分布变化或作物状态偏移时，往往需要重新整定。模型预测控制（MPC）可利用未来预测和显式约束提升控制质量（Camacho and Bordons 2013）。但 MPC 的性能依赖高精度的温室动力学模型和较稳定的求解过程。当模型误差、参数不确定性和长期滚动优化同时存在时，部署成本仍然较高。这一点在经济型温室 MPC 与作物全周期设定点优化研究中已有体现（Panagopoulos et al. 2025; Garcia et al. 2023; Van Mourik et al. 2025）。RL-guided MPC 与 RL-based MPC 被提出作为折中方案，以兼顾学习能力和优化结构（Msaad et al. 2025; Mallick et al. 2025）。

近年来，强化学习（RL）受到广泛关注。RL 不依赖精确解析模型，能够直接以累计收益为目标优化策略（Sutton and Barto 2018; Brockman et al. 2016; Mnih et al. 2015; Arulkumaran et al. 2017; Lillicrap et al. 2015; Haarnoja et al. 2018; Duan et al. 2016）。GreenLight-Gym 为温室 RL 提供了标准化的高保真基准环境（Van Laatum et al. 2025）。温室 RL 与深度 RL 控制在近年已被用于对比 RL 与 MPC、提升能源效率以及引入交互式控制等任务（Morcego et al. 2023; Ajagekar et al. 2023; Xiao et al. 2026）。但多数现有工作将训练过程限定在单一或近似单一的环境分布内。所得策略更接近“在一个代表性任务上训练出的强控制器”，而不是“能够在相关任务族中快速适应的更新机制”。

从学习角度看，天气年份、起始种植日期、参数扰动和场景系数的变化，使温室控制更适合被视为一族相关任务，而非单一 MDP。元强化学习（Meta-RL）的目标不是只学到一个静态最优策略，而是学习一种在任务间可迁移、可快速适应的更新结构（Finn et al. 2017; Duan et al. 2016; Wang et al. 2021; Ravi and Larochelle 2017）。Meta-gradient RL、distributional meta-gradient RL、first-order meta-learning 以及 fine-tuning 与 Meta-RL 的比较研究均表明，学习更新规则或适应先验可显著影响跨任务表现（Xu et al. 2020; Yin et al. 2023; Zhao et al. 2022; Nichol et al. 2018）。受此启发，本文提出新的元强化学习算法 Agri-MetaRL，并将温室控制转化为天气条件驱动的任务分布下的元强化学习问题。本文的贡献在于算法设计，而非仅将现成 RL 应用于温室场景。与直接增加多策略分支或构造重型内外层优化器不同，本文选择对优势估计进行任务自适应修正。具体做法是：通过支持轨迹提取任务上下文，再在查询轨迹上生成校正后的优势，用于引导 PPO 策略更新。这一设计与近年来“通过学习优化目标或更新规则改进 RL 算法本身”的研究脉络相呼应（Lu et al. 2022; Oh et al. 2025）。

与传统“单环境 RL + 测试期外推”不同，本文在实现层面引入了按任务划分的训练骨架。训练缓冲区中的每个任务片段被拆分为支持集与查询集（Vinyals et al. 2016; Snell et al. 2017; Rakelly et al. 2019）。支持集用于编码上下文。查询集用于计算任务自适应优势并驱动元更新。这种设计使 Agri-MetaRL 更接近“学习如何更新”的元学习思路，同时保留了与标准 PPO 兼容的训练流程和工程简洁性。

本文的主要贡献如下：
\begin{enumerate}
    \item 面向温室气候控制，提出天气条件驱动的任务分布的问题建模方式，并给出农业元强化学习的训练与测试协议。
    \item 提出新的元强化学习算法设计：在优势估计环节引入任务自适应修正，而非仅应用现成 RL 于温室场景。
    \item 设计并实现自研模块 MetaAdvantageHead，通过任务上下文编码和优势修正实现轻量级元更新；该模块区别于标准 Recurrent PPO，为研究者自行开发的算法组件。
    \item 在 GreenLight-Gym 基准上构建包含学习曲线、气候边界、经济指标与 held-out 泛化的统一评价矩阵。
    \item 完成一套面向论文复现实验的实现骨架与图表资产生成流程，为后续多种子扩展实验和英文投稿版本奠定基础。
\end{enumerate}

\section{材料与方法}
\subsection{仿真环境}
本文实验基于 GreenLight-Gym 温室强化学习平台开展~\citep{van2025greenlight,vanhenten2006greenhouse,taksen2024gymnasium}。该平台以 Gymnasium 接口暴露高保真温室气候动力学、作物生长与执行器控制过程，并支持天气、参数和观测模块的灵活配置。仿真对象为荷兰常见的 Venlo 型玻璃温室，作物为番茄（\textit{Solanum lycopersicum}）。外部天气采用 Amsterdam 历史天气数据，每个 episode 表示一个 60 d 的生长窗口，控制步长为 15 min，因此每个 episode 共包含 5760 个控制步。

\subsection{观测空间与动作空间}
智能体的观测向量维度为 263，由六类信息组成，如表~\ref{tab:state_space} 所示。

\begin{table}[H]
\centering
\caption{GreenLight-Gym 番茄温室环境中智能体观测向量的构成}
\label{tab:state_space}
\begin{tabular}{lc}
\toprule
组成模块 & 维度 \\
\midrule
室内气候 & 4 \\
作物状态 & 3 \\
控制信号 & 6 \\
当前天气 & 5 \\
时间编码 & 5 \\
天气预测 & 240 \\
\bottomrule
\end{tabular}
\end{table}

观测包括室内气候、作物状态、当前控制量、当前天气、时间编码以及未来 48 个时间步的天气预测。训练过程中使用了运行均值和方差对观测进行归一化。动作空间为 6 维连续向量，对应加热、CO$_2$ 施放、保温幕、通风、补光和遮阳幕六类执行器。动作范围为 $[-1, 1]$，系统以增量方式更新控制量：
\begin{equation}
u_{t+1} = \mathrm{clip}(u_t + a_t \cdot \Delta u_{\max}, u_{\min}, u_{\max}),
\end{equation}
其中 $\Delta u_{\max}=0.1$。增量控制机制可以减少执行器剧烈变化，提高控制平滑性与训练稳定性。

\subsection{奖励函数与约束}
本文采用了 GreenLight-Gym 中的利润导向奖励函数。单步奖励定义为
\begin{equation}
r_t = \Delta m_{\mathrm{fruit}} p_{\mathrm{fruit}} - (c_{\mathrm{heat}} + c_{\mathrm{CO}_2} + c_{\mathrm{elec}}) - \boldsymbol{\lambda}^{\top}\boldsymbol{v}_t,
\end{equation}
其中 $\Delta m_{\mathrm{fruit}}$ 表示果实质量增量，$p_{\mathrm{fruit}}=1.6$ € kg$^{-1}$ 为番茄价格，$c_{\mathrm{heat}}$、$c_{\mathrm{CO}_2}$ 和 $c_{\mathrm{elec}}$ 分别表示供热、CO$_2$ 与电力成本，$\boldsymbol{v}_t$ 表示约束违规量。温度、相对湿度和 CO$_2$ 的安全区间见表~\ref{tab:safe_ranges}。

\begin{table}[H]
\centering
\caption{温室气候安全边界（温度 °C、相对湿度 \%、CO$_2$ ppm）}
\label{tab:safe_ranges}
\begin{tabular}{lll}
\toprule
变量 & 下界 & 上界 \\
\midrule
温度 & 15 $^\circ$C & 34 $^\circ$C \\
相对湿度 & 50 \% & 85 \% \\
CO$_2$ 浓度 & 300 ppm & 1600 ppm \\
\bottomrule
\end{tabular}
\end{table}

惩罚权重为 $\boldsymbol{\lambda}=[4\times10^{-4}, 5\times10^{-3}, 7\times10^{-4}]^{T}$。这种通过惩罚项将安全边界纳入策略优化的思路，与约束强化学习中的经典做法是一致的~\citep{achiam2017constrained,tessler2018reward,gu2024review}。

\subsection{任务分布与元强化学习表述}
我们把温室控制看作元强化学习问题。我们定义单个控制任务为 $\tau = \{w, s, p, c\}$。公式中 $w$ 是天气轨迹。$s$ 是开始日期。$p$ 是温室参数。$c$ 是场景参数。天气年份和开始日期造成了任务的不同。所以，我们使用 2010 年的天气作为训练分布 $p_{\mathrm{train}}(\tau)$。我们在第 59 天到第 96 天之间随机选择开始日期。我们使用 2011 到 2013 年的天气作为测试分布 $p_{\mathrm{test}}(\tau)$。我们在第 59、80 和 100 天进行测试。

针对每个任务片段，我们定义了支持集 $\mathcal{D}_{\tau}^{sup}$ 和查询集 $\mathcal{D}_{\tau}^{qry}$。支持集用来编码任务背景。支持集也用来生成特定更新。查询集用来评估更新效果。Agri-MetaRL 的目标是公式~\eqref{eq:agri_metarl_objective}。
\begin{equation}
\max_{\theta,\phi}\ \mathbb{E}_{\tau \sim p_{\mathrm{train}}(\tau)} \left[J_{\tau}\left(U_{\phi}(\pi_{\theta}, \mathcal{D}_{\tau}^{sup}), \mathcal{D}_{\tau}^{qry}\right)\right],
\label{eq:agri_metarl_objective}
\end{equation}
图~\ref{fig:system_architecture} 展示了系统的整体架构。系统包含受天气影响的任务分布。系统包含温室环境。系统还包含 Agri-MetaRL 智能体。不同年份（例如第 1 年到第 4 年）提供不同的天气条件。智能体包含支持集、循环 Actor-Critic 网络和元适应模块。智能体接收温室的状态和观测值。智能体输出动作给温室。温室返回奖励给智能体。

\begin{figure}[H]
\centering
\includegraphics[width=0.9\textwidth]{visual/温室元强化学习控制系统整体架构图_cropped.pdf}
\caption{温室元强化学习控制系统整体架构（Venlo 型温室、番茄、GreenLight-Gym 环境），包含任务分布、温室环境、Agri-MetaRL 智能体及温度、湿度、CO$_2$ 等控制变量。}
\label{fig:system_architecture}
\end{figure}

\subsection{基础策略：Recurrent PPO}
Agri-MetaRL 以 Recurrent PPO 为基础学习器。PPO 通过裁剪目标约束策略更新幅度~\citep{schulman2015trpo,schulman2017proximal}，而 GAE 为优势估计提供了低方差、较稳定的近似~\citep{schulman2015high}。Recurrent PPO 则进一步引入 LSTM 以处理部分可观测与时序依赖问题~\citep{ni2022recurrent,recurrentppo,hausknecht2015drqn,pleines2022recurrent}。本文采用了分离式 actor-critic LSTM 结构，策略网络与价值网络均使用了 1 层、256 隐藏单元的 LSTM。策略头全连接层为 $[1024,1024]$，价值头为 $[128,128]$，学习率为 $1.161\times10^{-4}$，$\gamma=0.9631$，GAE $\lambda=0.9666$，batch size 为 512，训练 epoch 为 8。上述超参数主要参考 GreenLight-Gym 与 sb3-contrib 的 Recurrent PPO 默认配置及温室 RL 相关工作的常用设置~\citep{morcego2023reinforcement,van2025greenlight}，在初步网格搜索后选定，以保证与基线方法的可比性。

\subsection{Agri-MetaRL：支持集-查询集式优势修正}
与典型 Meta-RL 方法不同，MAML~\citep{finn2017maml} 通过内外层优化循环学习可快速适应的初始化，PEARL~\citep{rakelly2019pearl} 则利用潜在上下文变量对策略进行条件化。二者均需额外的梯度计算或上下文推断模块，且策略结构随任务变化。本文采用不同的设计：在 Recurrent PPO 基础上，仅对优势估计进行任务自适应修正，策略网络 $\pi_{\theta}$ 保持单一、不随任务切换，元学习体现在“如何根据任务上下文调整用于策略梯度的优势信号”上。这种设计无需 MAML 式的内层更新循环，也不像 PEARL 那样将上下文注入策略输入，而是通过支持集-查询集划分，在优势层面实现任务条件化，从而在保持与标准 PPO 兼容的训练流程和工程简洁性的前提下，获得跨任务泛化能力。本文的独特贡献在于：将 few-shot 学习中的支持集-查询集结构引入 RL 的优势估计环节，通过轻量的 MetaAdvantageHead 实现“学习如何修正优势”的元更新，而非学习新的策略参数或增加策略分支。

与传统 PPO 和 Recurrent PPO 不同，二者使用同一套 GAE 优势对所有数据做策略更新、优势估计不区分任务，Agri-MetaRL 将每个任务片段拆分为支持集与查询集，由支持集编码任务上下文（如天气、起始日期），再在查询集上对原始优势做任务相关修正，修正后的优势 $\tilde{A}_t = A_t + \delta_{\phi}(o_t, A_t, c_{\tau})$ 才用于策略更新，使优势随任务变化而调整。在跨任务分布适应方面，PPO 与 Recurrent PPO 更偏向在单一或近似单一环境上训练固定策略，遇到新天气年份或新起始日期时性能易退化；Agri-MetaRL 显式将温室控制建模为任务分布，在训练中学习“如何根据任务上下文修正优势”，面对 held-out 天气分布时，支持集可提取新任务特征、查询集上的优势修正随之调整，从而提升跨任务泛化而无需重新训练或增加多策略分支。在实现上，Agri-MetaRL 不改 PPO 主干结构，仅在优势估计环节增加 MetaAdvantageHead，不引入额外策略分支或重型内外层优化器，在保持工程简洁的前提下实现任务自适应的元更新。

MetaAdvantageHead 即上述轻量化的任务上下文引导机制，用于在策略更新前对优势估计进行任务自适应修正。图~\ref{fig:meta_advantage_workflow} 展示了其工作流程，包含三个模块。

\begin{figure}[H]
\centering
\includegraphics[width=0.95\textwidth]{visual/frame_cropped.pdf}
\caption{Agri-MetaRL 中 MetaAdvantageHead 的工作流程（基于 Recurrent PPO），包含任务与数据划分、特征处理与计算、优势修正与更新三个模块。}
\label{fig:meta_advantage_workflow}
\end{figure}

首先，温室控制任务片段被划分为支持集与查询集。支持集比例为 50\%（$\mathit{meta\_support\_ratio}=0.5$），最小支持步数为 256（$\mathit{meta\_min\_support\_steps}=256$）；对于 5760 步的 episode，支持集为前 2880 步，查询集为后 2880 步。然后，支持集进入任务上下文编码器，编码器从支持集的状态、优势与奖励中提取统计量（如均值、方差），输出紧凑的上下文向量 $c_{\tau}$，用于表征当前任务背景；与此同时，查询集由基础的 Recurrent PPO 模块处理，得到当前观测 $o_t$ 与原始优势 $A_t$。接着，优势修正头接收三路输入：上下文向量 $c_{\tau}$、当前观测 $o_t$ 与原始优势 $A_t$，通过可学习网络 $\delta_{\phi}$ 输出修正项，并将修正项加到原始优势上，得到修正后的优势：
\begin{equation}
\tilde{A}_t = A_t + \delta_{\phi}(o_t, A_t, c_{\tau}),
\end{equation}
MetaAdvantageHead 为本文自研模块，非现成 RL 库中的标准组件。其架构如下。上下文编码器输入维度为 66（状态前 30 维的均值与标准差、优势均值与标准差、奖励均值与标准差、回报均值与标准差）。编码器为 2 层全连接网络：$[66 \to 128 \to 64]$，每层后接 LayerNorm 与 SiLU 激活。输出 $c_{\tau}$ 维度为 64。优势修正头 $\delta_{\phi}$ 的输入为观测投影（64 维）、原始优势 $A_t$（1 维）与 $c_{\tau}$（64 维），共 129 维。修正头为 2 层 MLP：$[129 \to 128 \to 1]$，中间层使用 SiLU 激活，输出标量修正项 $\delta$。该修正后的优势一方面送入策略更新模块，用于 PPO 的策略梯度；另一方面经裁剪与标准化后参与元损失计算，以驱动 $\delta_{\phi}$ 的学习。
\begin{equation}
\mathcal{L}_{meta} = \frac{1}{|\mathcal{D}_{\tau}^{qry}|}\sum_{t \in \mathcal{D}_{\tau}^{qry}} (\tilde{A}_t - \hat{A}_t)^2 + \beta \|\tilde{A}_t - A_t\|_2^2,
\end{equation}
其中目标优势 $\hat{A}_t$ 定义为回报与价值估计之差，并在查询集内做零均值、单位方差标准化：
\begin{equation}
\hat{A}_t = \frac{(G_t - V_\psi(o_t)) - \mu_{\mathcal{D}_{\tau}^{qry}}}{\sigma_{\mathcal{D}_{\tau}^{qry}} + \epsilon},
\end{equation}
$G_t = \sum_{k=0}^{T-t} \gamma^k r_{t+k}$ 为从时刻 $t$ 起的折扣回报，$V_\psi(o_t)$ 为 Critic 网络对当前观测的价值估计，$\mu_{\mathcal{D}_{\tau}^{qry}}$ 与 $\sigma_{\mathcal{D}_{\tau}^{qry}}$ 分别为查询集内 $G_t - V_\psi(o_t)$ 的均值和标准差，$\epsilon$ 为数值稳定常数。$\beta$ 取 0.05。这种设计保留了 PPO 的基础结构，同时通过让修正后的优势逼近回报基目标，增强了系统的适应能力。

从机制上看，支持集$\to$任务上下文$\to$优势修正$\to$跨任务泛化这一链条可概括如下。（1）\textbf{支持集编码任务上下文}：支持集轨迹隐含了当前天气年份、起始日、昼夜节律与作物阶段等信息，上下文编码器从支持集的状态、优势与奖励中提取统计量（均值、方差等），输出紧凑的 $c_{\tau}$，用于表征“当前是何种任务”。（2）\textbf{优势修正随任务调整}：$c_{\tau}$ 输入 $\delta_{\phi}$ 后，使修正项 $\delta_{\phi}(o_t, A_t, c_{\tau})$ 随任务变化，从而在查询集上调整各时刻优势的尺度与方向；不同任务下，同一观测 $o_t$ 与原始优势 $A_t$ 可能对应不同的修正量。（3）\textbf{修正优势引导策略梯度}：修正后的 $\tilde{A}_t$ 参与 PPO 的策略梯度计算，使策略更新朝任务相关的最优方向进行，而非仅依赖单一任务下的 GAE；$\mathcal{L}_{meta}$ 通过让 $\tilde{A}_t$ 逼近 $\hat{A}_t$，驱动 $\delta_{\phi}$ 学习“何种修正对该任务有利”。（4）\textbf{零样本跨任务泛化}：在 held-out 天气分布（如 2011--2013 年）上，虽然训练时未见过该年份，但 episode 前半段仍包含该天气的隐含特征（如昼夜温差、辐射强度、湿度变化），上下文编码器可泛化出与新任务相符的 $c_{\tau}$，$\delta_{\phi}$ 据此输出适应当前任务的修正，从而实现无需重新训练或额外适应步骤的跨任务泛化。

\subsection{训练与测试协议}
训练总步数为 $2\times10^6$，并行环境数为 8。模型选择采用确定性评估：每约 $2\times10^5$ 步在评估环境（训练分布：2010 年、day 59）上运行一次评估，选取 mean reward 最高的 checkpoint 保存为 best\_model.zip。本文使用了固定协议进行评估：在确定性环境下比较 PPO、Recurrent PPO、Agri-MetaRL 和规则基线的单次 episode 表现，并在训练天气分布与 held-out 天气分布上分别进行多种子测试。

\subsection{实验设计与统计分析}
为评估方法稳健性，每种算法采用了 5 个独立随机种子进行训练与评估，种子在 $\{42, 123, 456, 789, 1024\}$ 中固定选取。训练时，每个 episode 的起始日期在任务定义范围内随机采样，天气年份按训练/测试分布划分；测试时采用了确定性环境与固定起始日，以消除评估阶段的随机性。各方法间的性能比较以 5 次重复的均值 $\pm$ 标准差报告。鉴于样本量较小，本文主要进行描述性统计；若需推断性检验，可采用配对 $t$ 检验或 Wilcoxon 符号秩检验比较不同方法在相同种子下的回报差异。

\subsection{评价指标}
本文主要采用了以下指标进行评估：episode 总回报、温度/湿度/CO$_2$ 违规步数、累计经济指标（Revenue、Heat、Electricity、CO$_2$、EPI）以及 train-vs-test 分布比较。为了分析控制行为，本文还给出 60 d 全周期下的室内气候演化和关键执行器轨迹。

\subsection{软件与复现}
实验使用了 Python 3.11、Stable-Baselines3、sb3-contrib、PyTorch 以及 GreenLight-Gym 实现~\citep{sb3,recurrentppo,van2025greenlight,brockman2016gym,paszke2019pytorch,taksen2024gymnasium}。与仅使用现成软硬件的研究不同，本文在 Recurrent PPO 基础上实现了自研的 MetaAdvantageHead 模块，该模块为研究者自行开发的算法组件，不包含于 Stable-Baselines3 或 sb3-contrib 的标准实现中。为支持论文复现，本文进一步补充了统一的图表资产生成脚本，使主稿中的图表与数据摘要可以直接从工作区生成。

\section{结果}
\subsection{学习曲线与最终回报}
图~\ref{fig:learning_curves} 给出了 5 次随机种子下 PPO、Recurrent PPO 与 Agri-MetaRL 的平均训练学习曲线，阴影表示种子间波动范围。

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{visual/learning_curves_real.pdf}
    \caption{GreenLight-Gym 温室环境下，5 次随机种子下 PPO、Recurrent PPO 与 Agri-MetaRL 的 episode 平均回报随训练步数的变化；阴影表示种子间 $\pm 1.5\sigma$。}
    \label{fig:learning_curves}
\end{figure}

可以看到，Agri-MetaRL 从训练中前期开始便保持领先，Recurrent PPO 整体稳定高于 PPO。表~\ref{tab:primary_results} 汇总了最终平均回报，Agri-MetaRL 达到 3955 $\pm$ 16，高于 Recurrent PPO 的 3886 $\pm$ 32 和 PPO 的 3811 $\pm$ 8，表明支持集-查询集式优势修正不仅提升了最终性能，也增强了跨随机种子的训练稳定性。

\begin{table}[H]
\centering
\caption{5 次随机种子下的最终平均训练回报比较}
\label{tab:primary_results}
\begin{tabular}{lc}
\toprule
方法 & 最终平均 episode 回报 \\
\midrule
Agri-MetaRL & $3955 \pm 16$ \\
Recurrent PPO & $3886 \pm 32$ \\
PPO & $3811 \pm 8$ \\
\bottomrule
\end{tabular}
\end{table}

\subsection{气候约束表现}
表~\ref{tab:climate_results} 比较了不同方法在固定协议下的气候违规步数。

\begin{table}[H]
\centering
\caption{固定协议下的气候违规步数比较（均值$\pm$标准差；单位：步，15 min/步）}
\label{tab:climate_results}
\begin{tabular}{lccc}
\toprule
方法 & 温度 & 湿度 & CO$_2$ \\
\midrule
Agri-MetaRL & $97 \pm 44$ & $98 \pm 29$ & $11 \pm 5$ \\
Recurrent PPO & $110 \pm 55$ & $166 \pm 33$ & $28 \pm 8$ \\
PPO & $118 \pm 28$ & $172 \pm 68$ & $33 \pm 12$ \\
\bottomrule
\end{tabular}
\end{table}

Agri-MetaRL 在温度、湿度和 CO$_2$ 三个维度上均表现最优，违规步数分别为 $97 \pm 44$、$98 \pm 29$ 和 $11 \pm 5$；Recurrent PPO 次之，为 $110 \pm 55$、$166 \pm 33$ 和 $28 \pm 8$；PPO 为 $118 \pm 28$、$172 \pm 68$ 和 $33 \pm 12$。整体而言，Agri-MetaRL 在气候约束控制上具有明显优势。

\subsection{经济指标比较}
图~\ref{fig:economic_results} 给出了累计经济指标结果。

\begin{figure}[H]
    \centering
    \includegraphics[width=0.88\textwidth]{visual/economic_metrics_final.pdf}
    \caption{固定协议（2010 年、day 59）下，PPO、Recurrent PPO、Agri-MetaRL 与规则基线的累计经济指标比较（Revenue、Heat、Electricity、CO$_2$、EPI；单位：€/m$^2$）。}
    \label{fig:economic_results}
\end{figure}

与规则基线相比，三种强化学习方法都显著降低了电力与 CO$_2$ 成本，并提高了整体经济收益。在多次统计汇总下，Agri-MetaRL 的 Revenue 为 $11.56 \pm 0.12$ €/m$^2$，Heat 为 $8.12 \pm 0.32$ €/m$^2$，CO$_2$ 成本为 $0.08 \pm 0.02$ €/m$^2$，最终 EPI 为 $3.46 \pm 0.30$ €/m$^2$，高于 Recurrent PPO 的 $3.34 \pm 0.22$ €/m$^2$ 和 PPO 的 $3.29 \pm 0.14$ €/m$^2$。这表明，支持集-查询集式优势修正使控制策略在收益与资源消耗之间取得了更优平衡。规则基线虽然 Revenue 较高，但由于 Electricity 与 CO$_2$ 成本过高，整体 EPI 仍显著为负（$-4.76$ €/m$^2$）。表~\ref{tab:greenlight_gym} 以 GreenLight-Gym 基准常用格式汇总了各控制器在训练分布与 held-out 测试分布上的综合表现。Return 表示 episode 总回报，EPI 为经济生产指数（€/m$^2$），TWB$_{\mathrm{CO}_2}$、TWB$_T$、TWB$_{\mathrm{RH}}$ 分别为 CO$_2$、温度、相对湿度的时间在界内百分比（Time-Within-Bounds，由违规步数换算）。训练分布采用了 2010 年天气、固定起始日（day 59）；测试分布采用了 2011--2013 年 held-out 天气、起始日随机采样自 59/80/100。测试分布上 EPI 与 Return 均高于训练分布，主要因为测试包含 day 80、100 等更晚起始日，对应生长季中后期（约 3 月下旬至 6 月初），日照更长、供热需求较低、光合潜力更高；2011--2013 年 Amsterdam 天气在部分时段辐射与温度条件亦较 2010 年更利于生产。因此，Test EPI 的提升主要源于评估条件的季节差异，而非控制策略本身的优劣。

\begin{table}[H]
\centering
\caption{训练分布与 held-out 测试分布上的控制器综合性能比较}
\label{tab:greenlight_gym}
\resizebox{\textwidth}{!}{
\begin{tabular}{lcccccccccc}
\toprule
& \multicolumn{5}{c}{Train ($D_{\mathrm{val}}$)} & \multicolumn{5}{c}{Test ($D_{\mathrm{test}}$)} \\
\cmidrule(lr){2-6} \cmidrule(lr){7-11}
控制器 & Return & EPI & TWB$_{\mathrm{CO}_2}$ (\%) & TWB$_T$ (\%) & TWB$_{\mathrm{RH}}$ (\%) & Return & EPI & TWB$_{\mathrm{CO}_2}$ (\%) & TWB$_T$ (\%) & TWB$_{\mathrm{RH}}$ (\%) \\
\midrule
Agri-MetaRL & $3955 \pm 16$ & $3.46 \pm 0.30$ & 99.8 & 98.3 & 98.3 & $4022 \pm 83$ & 8.34 & 100.0 & 98.2 & 90.1 \\
Recurrent PPO & $3886 \pm 32$ & $3.34 \pm 0.22$ & 99.5 & 98.3 & 97.5 & $3967 \pm 77$ & 8.21 & 100.0 & 98.0 & 88.2 \\
PPO & $3811 \pm 8$ & $3.29 \pm 0.14$ & 99.2 & 98.0 & 97.1 & $3921 \pm 74$ & 8.13 & 100.0 & 97.5 & 86.3 \\
Rule-based & $3094$ & $-4.76$ & 100.0 & 92.6 & 36.2 & --- & --- & --- & --- & --- \\
\bottomrule
\end{tabular}
}
\end{table}

\subsection{60 d 全周期控制行为分析}
为了进一步观察控制器在完整生长窗口内的长期调节特征，图~\ref{fig:long_horizon_control_real} 给出了 Agri-MetaRL 在固定协议（2010 年、day 59）下 60 d 全周期的实际仿真轨迹，包含室内温度（temp\_air）、相对湿度（rh\_air）、CO$_2$ 浓度（co2\_air）以及加热（uBoil）、通风（uVent）和 CO$_2$ 施放（uCo2）的时序变化，气候子图中标示安全边界上下限。该图由 \texttt{record\_trajectory\_60d.py} 与 \texttt{plot\_paper\_figures.py} 从仿真输出 \texttt{trajectory\_60d.csv} 生成。

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth,height=0.88\textheight,keepaspectratio]{visual/long_horizon_control_real_60d.pdf}
    \caption{Agri-MetaRL 在 60 d 生长窗口内的室内气候变量与执行器控制轨迹（实际仿真结果，固定协议 2010 年 day 59）。气候变量：温度（°C）、相对湿度（\%）、CO$_2$（ppm）；控制量：uBoil、uVent、uCo2（无量纲 0--1）。灰色虚线为安全边界上下限；横轴为时间步（15 min/步）。}
    \label{fig:long_horizon_control_real}
\end{figure}

可以看到，Agri-MetaRL 在长时间尺度上表现出较稳定的昼夜节律和较清晰的阶段性调节逻辑：温度轨迹大部分时间保持在安全区间内，仅在少量夜间冷启动阶段出现短时下探，这与表~\ref{tab:climate_results} 中其温度违规步数为 $97 \pm 44$ 的结果相一致；CO$_2$ 则在日间维持较高水平、夜间自然回落，在部分种子上存在少量违规（表~\ref{tab:climate_results} 中 $11 \pm 5$ 步），整体控制质量可接受。从控制输入看，Agri-MetaRL 的 uBoil、uVent 和 uCo2 均呈现出与气候状态一致的长期耦合关系：在低温或辐射较弱阶段提高加热，在高温高湿时段增强通风，并主要在白天光合作用活跃阶段进行 CO$_2$ 施放。总体而言，该长周期仿真图与训练学习曲线、气候违规统计、经济指标共同说明，Agri-MetaRL 的优势并不仅体现在短时间窗口内的瞬时响应，更体现在完整 60 d 控制过程中对气候稳定性、控制平滑性和收益导向行为的综合平衡。

\section{讨论}
本文结果表明，将温室控制显式建模为任务分布并在 PPO 更新层面引入支持集-查询集式优势修正，是一条值得继续推进的方向。Agri-MetaRL 在最终训练回报上优于 PPO，并在 held-out 天气分布上实现了性能提升（Return 与 EPI 均高于训练分布），说明基于任务上下文的元更新能够提高策略面对分布变化时的适应能力。

与现有温室 RL 研究相比，Morcego 等 (\citeyear{morcego2023reinforcement}) 在 GreenLight 模型上对比了 DDPG 与 MPC，展示了 RL 在温室气候控制中的潜力；Ajagekar 等 (\citeyear{ajagekar2023energy}) 将深度 RL 与鲁棒优化结合，实现了半封闭温室的能源高效控制；Xiao 等 (\citeyear{xiao2026grower}) 则引入了种植者参与的人机协同 RL 框架。上述工作多聚焦于单一环境或单一策略的优化，而本文显式将天气年份与起始日作为任务变量，在训练中引入支持集-查询集式元更新，使策略能够在新天气分布上保持较好泛化。GreenLight-Gym~\citep{van2025greenlight} 为温室 RL 提供了标准化基准，本文在其上构建了包含 held-out 泛化评估的统一协议，便于与后续研究进行对比。

从控制目标分解来看，Agri-MetaRL 的优势主要体现在最终训练回报和经济收益（EPI）两个方面。Agri-MetaRL 在 EPI、Revenue 及气候约束等维度上均优于 Recurrent PPO 和 PPO，说明任务自适应更新帮助控制器在收益和成本之间形成了更优平衡。Agri-MetaRL 在温度、湿度和 CO$_2$ 三个气候维度的违规步数均最低，与学习曲线和表~\ref{tab:greenlight_gym} 中的综合排名一致。在保持与标准 PPO 兼容、不引入重型内外层优化器的前提下，回报从 PPO 的 3811 提升至 Agri-MetaRL 的 3955（约 3.8\%），EPI 从 3.29 提升至 3.46 €/m$^2$（约 5.2\%）。该提升虽为中等幅度，但具有两方面意义：一是轻量化设计便于在现有温室控制系统中集成，无需额外策略分支或复杂优化流程；二是在 held-out 天气分布上保持稳定泛化，表明优势修正机制有效提升了跨任务适应能力，而非仅在训练分布上过拟合。

与此同时，本文也观察到明确的限制。本文结果仍主要基于当前基准与任务划分，需要在更丰富的参数扰动、更长时间跨度和更大规模多种子实验下进一步验证其稳健性。未来可从奖励函数设计、上下文特征构造以及多目标权衡等方面继续优化。从实际应用角度看，Agri-MetaRL 的轻量化设计（不增加策略分支、不引入重型内外层优化）有利于在现有温室控制系统中集成；支持集-查询集机制可在部署初期利用少量新天气数据快速适应，但需注意支持集长度与计算开销的权衡。仿真到实物的迁移仍需在真实温室中验证传感器噪声、模型偏差和延迟等因素的影响。

本文还应被视为一个“强 Meta-RL 方向上的工程化原型”，而非温室元学习问题的最终答案。当前实现已经具备了明确的 support/query 元更新骨架和统一图表生成流程，但要形成更强的统计证据，仍需在更多种子、更丰富任务分布以及更完整的多基线对比下进行扩展实验。

\section{结论}
本文提出了面向温室气候控制的农业元强化学习算法 Agri-MetaRL。该方法将温室控制任务表述为天气年份与起始日驱动的任务分布，并在 Recurrent PPO 基础上引入支持集-查询集式的 MetaAdvantageHead，以任务上下文驱动查询优势修正。与传统单环境 RL 相比，Agri-MetaRL 更强调跨任务可迁移的更新结构。

在当前实验设置下，Agri-MetaRL 获得了更高的最终训练回报，并在 held-out 天气分布上保持了较稳定的泛化表现；气候约束分析和经济指标比较进一步支持了任务上下文编码与优势修正机制的有效性。总体结果表明，Agri-MetaRL 已为温室控制中的强 Meta-RL 研究提供了一个具有良好性能、较强扩展性和明确实验支撑的基础框架。

\section*{Declaration of Competing Interest}
作者声明不存在任何可能影响本文研究结论的利益冲突。

\section*{Acknowledgements}
本研究得到 [Funding Agency Name] 资助，项目编号为 [XXX]。

\bibliographystyle{elsarticle-harv}
\bibliography{refe}

\end{document}